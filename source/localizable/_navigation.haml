-# .bla{ style: 'height: 600px; width: 200px; background-color: red;' }
.nav-triangle-topright.as-shadow
.nav-triangle-bottomleft.as-shadow
.nav-box.as-shadow
.nav-triangle-topright
.nav-triangle-bottomleft
.nav-box
.nav-options
  %ul
    - t('topic').keys.each do |topic_sym|
      %li
        = link_to t("topic.#{topic_sym}.title"), "/#{topic_sym != :home ? topic_sym.to_s+'.html' : ''}", class: "#{topic_sym == @current_page ? 'is-current' : ''}"
.nav-logo.header-font= t('defaults.logo')
